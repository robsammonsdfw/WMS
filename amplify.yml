version: 1
frontend:
  phases:
    build:
      commands:
        - echo "window.APP_CONFIG = { API_KEY: '$API_KEY' };" > env-config.js
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
