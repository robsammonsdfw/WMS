version: 1
frontend:
  phases:
    # No build commands are needed for this static site deployment.
    build:
      commands: []
  artifacts:
    # Serve all files from the root directory.
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
